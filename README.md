# Kshetra

Digital farm book: map plots, record work, and see profit per field.

```bash
npm install
npm run dev
```

Open the local URL Vite prints. Data stays in this browser. Use Settings → Reload demo farm to restore sample Nashik plots.
# farm-admin
