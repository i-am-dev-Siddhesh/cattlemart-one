# Local default is SQLite so the app runs without Docker.
# For PostgreSQL: postgresql://farmos:farmos@localhost:5432/farmos
DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-me"
AUTH_URL="http://localhost:3000"
MAPBOX_TOKEN=
WEATHER_API_KEY=
